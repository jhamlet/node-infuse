/**
 * This is used as a template for inserting a required module.
 */
function foo () {
    var module = require.modules[MODULE_ID],
        exports = module.exports;
    MODULE
    require.clear(MODULE_ID);
}